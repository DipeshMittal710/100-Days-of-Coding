#include <stdio.h>

int main(){
    int a,b,c,per;
    printf("Enter length:\n");
    scanf("%d", &a);
    printf("Enter breadth:\n");
    scanf(" %d", &b);
    c = a*b;
    per = (a+b)2;
    printf("The area is %d\n", c);
    printf("The perimeter is %d\n", per);

        return 0;
        
    }
